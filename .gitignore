# Environment variables (contains sensitive API keys)
.env
.env.local

# Dependencies
node_modules/

# Runtime data（npc-prices.json 保留，是启动必需的数据）
data/favorites.json
data/history.json
server.log

# Deployment package (阿里云 FC 上传用，不入库)
bazaar-fc.zip

# Optional: ignore package-lock if you prefer yarn
# package-lock.json

# EdgeOne 函数运行时数据（本地测试可能生成）
cloud-functions/data/
